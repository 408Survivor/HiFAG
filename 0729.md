# HiFAG：一个模型的诞生与取舍（截至 2026-07-29）

> HiFAG（Hierarchical Facial-Audio Graph Network）：在 AFGNN 的 68-landmark 细粒度
> 面部图之上增加 9-region 粗粒度面部图，做层级化面部建模，用于 D-Vlog 抑郁检测。
> 本文按时间线记录这个项目从立项到 A10 的每一步决策：当时面对什么、选了什么、
> 结果如何、又逼出了什么新决策。

---

## 起点：为什么要做 HiFAG

前置项目有两个。AFGNN 用 68 个面部 landmark 构建时空图做抑郁检测，最强配置
Test AUC 0.797。之后的 SFAF 尝试堆叠更复杂的架构（GRU + Transformer，80 万参数），
结果在 800 样本的小数据上全面失败，留下一条血泪教训：

> **小数据上，架构复杂度是负收益；只有带来新信息的输入表示才是正收益。**

于是我们问自己：68 点细图天生学不到什么"新信息"？答案有三类——
**区域级整体状态**（睁眼面积、嘴唇张开度，需要区域内所有点协同计算）、
**左右不对称性**（左眼 vs 右眼的运动差异，抑郁文献里的常见线索）、
**区域间协调**（下颌-嘴唇耦合，9 节点图上是相邻边，68 点图上要跨很多跳）。

HiFAG 的立项假设就是：用一个 9-region 粗粒度图，把这三类细图难学的信号
显式喂给模型。

## 取舍一：粗节点里放什么——手工描述子，而不是可学习模块

粗图节点内容的候选方案有三个：可学习聚合（用 GNN/注意力从 landmark 学出来）、
预训练特征、手工描述子。按 SFAF 教训，前两个都要花参数预算去"学一个表示"，
在 800 样本上大概率过拟合或学到冗余。

**选择：零参数手工描述子**，每区域每帧 10 维——质心、面积、散布度、平均速度、
速率、运动方差、左右对称性。确定性计算，不消耗任何参数预算，
且每一维都有明确的临床语义。

## 取舍二：先统计验证，再写模型——信号不存在就不建模

模型代码一行没写，先做了单变量统计检验（961 样本，180 个聚合特征）：

- 113/180 个特征 p < 0.05——**信号确认存在**；
- 最强特征 `std(inner_mouth.spread)` 单变量 AUC = 0.692，p = 1.5e-27；
- 模式高度一致：top 特征全是**时序标准差**，方向全部是抑郁组更低——
  "面部动态范围收窄"，与临床文献吻合；
- 对称性特征 |d| ≈ 0.5，正中立项假设。

这个结果还有一个当时没意识到、后来才显出重要性的细节：**主信号的粒度是
"整个视频的统计量"，不是逐帧事件**。记住这一点，它是第六章阴性结果的伏笔。

## 取舍三：工程先行——可消融、可复现、失败可记录

正式建模前定下一套工程纪律（全部落实）：模态/融合/特征组/边拓扑全部做成
配置开关；实验目录自动编号存档；特征维度入口断言（SFAF 曾因静默丢弃特征
误导了一个实验结论）；每个结论必须 5 个 seed + 配对 t 检验，不看单次运行。
这套纪律在后面两次"自我纠错"中收回了全部成本。

## A2/A3：粗图能用，但先抓出一个基线错误

- **A2（粗图单独，15.8K 参数）：0.6599 ± 0.0095**——粗图有真实判别力；
- **A3（细+粗 concat）：0.6709 ± 0.0045**，配对 A3−A2 均值 +0.011（p≈0.086，
  边缘显著），且波动减半。

表面看 0.67 远低于 AFGNN 基线 0.797，项目一度蒙上阴影。核查基线配置后
发现一个关键事实：**0.797 的基线含音频模态，纯面部基线只有 0.657**。
A2/A3 其实都已略超合理对照。

> 教训：引用任何 baseline 数字之前，先核对其配置文件的每一个开关。

## A7/A8：补上音频，并意外发现 concat 在拖累训练

下一步明确：加音频，对齐完整基线。**A7（细+粗+音频，concat）：0.7756 ± 0.0085**，
音频带来 +0.105 的巨大增益，但距 0.797 还差 0.021。

A7 暴露了一个异常：3/5 个 seed 的最佳验证集表现出现在第 3~5 个 epoch——
**模型还没训练充分就被早停了**。怀疑对象是融合方式：concat 是弱融合，
可能压制了强模态分支的学习。

验证这个怀疑只需一个改动：换 cross-attention 融合。
**A8：0.7863 ± 0.0088，配对 A8−A7 均值 +0.0106，5/5 seeds 全正，t≈4.70（p<0.01）**。
训练曲线同步恢复健康（最佳 epoch 后移到 13~41）。与基线差距收窄到 −0.011，
落在基线 1 个标准差内——**基本追平**。

> 取舍启示：怀疑要有，但更要设计得出"只改一个变量"的实验去验证它。

## A9：追平之后，必须回答"粗图到底贡献了什么"

A8 追平了基线，但此刻粗图是搭在细图+音频的便车上还是真有贡献？做了归因消融
A9（A8 去掉粗图）：**0.7805 ± 0.0123**。配对 A8−A9 = **+0.0058，仅 2/5 seeds 为正，
不显著**。

但数据里藏着另一面：无粗图的 A9 seed 间波动 0.0123，有粗图的 A8 是 0.0088。
**粗图的均值增量存疑，稳定性收益却是实的。**

## A10：最后一次尝试放大增量——FiLM 层级交互，阴性结果

并联不行，就换结构：让粗图节点嵌入按"帧 + 区域归属"FiLM 调制细图的几何输入
（zero-init 起步，保证不退化）。这是立项时规划的"第二阶段层级交互"。

**A10：0.7824 ± 0.0066，配对 A10−A8 = −0.0039，不显著——阴性结果。**

回到第二章那个伏笔：粗描述子的主信号是**时序标准差**，是样本级全局属性
（"整个视频表情动态范围收窄"），不是逐帧的局部事件。帧级 FiLM 调制在信号
粒度上就不匹配。这条路从一开始就可能走不通，只是当时没有这样解读 sanity 数据。

## 现状：粗图的价值重新定位

两轮冲击均值增量均未成功，但粗图的三块立足点反而更清楚了：

1. **判别力真实**：15.8K 参数单独到 0.66，超过纯面部基线；
2. **稳定性一致可复现**：三轮完整模态实验，波动 0.0123（无粗图）→
   0.0088（并联）→ 0.0066（FiLM）——小数据上，结论的可靠性本身就是贡献；
3. **可解释性**：描述子直接对应临床概念，这是纯学习分支给不了的叙事。

## 下一步：把"粗图学到了什么"讲清楚

A4~A6 消融已预登记（exp_31~45），全部以 A2 为基底、5 seeds、配对检验：

| 对比 | 回答的问题 |
|------|-----------|
| A2 − A4（去对称性特征） | 对称性信号的净贡献 |
| A2 − A5（只留几何特征） | 运动/动态特征的净贡献 |
| A6 − A2（全连接 vs 解剖学边） | 解剖学先验是否有用 |

之后按结果择路：尝试样本级粗信号注入（针对粒度问题），
或接受"粗图 = 稳定性 + 可解释性组件"的定位，转入论文写作。

---

## 附：全部结果速查

| 配置 | 结构 | Test AUC (5 seeds) | 一句话结论 |
|------|------|--------------------|-----------|
| AFGNN 基线 | 细图+音频+x-attn | 0.797 ± 0.011 | 对照 |
| A2 | 粗图单独 | 0.6599 ± 0.0095 | 粗图有真实判别力 |
| A3 | 细+粗 concat | 0.6709 ± 0.0045 | 略超纯面部基线 0.657 |
| A7 | +音频 concat | 0.7756 ± 0.0085 | 音频 +0.105；concat 欠训练 |
| **A8** | +音频 x-attn | **0.7863 ± 0.0088** | 最佳；基本追平基线 |
| A9 | A8 去粗图 | 0.7805 ± 0.0123 | 粗图均值增量 n.s.，稳定性减半波动 |
| A10 | A8 + FiLM | 0.7824 ± 0.0066 | 阴性；波动最小 |
