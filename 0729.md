# HiFAG 项目进展汇报（截至 2026-07-29）

> HiFAG（Hierarchical Facial-Audio Graph Network）：在 AFGNN 的 68-landmark 细粒度面部图之上，
> 增加 9-region 粗粒度面部图做层级化面部建模，用于 D-Vlog 抑郁检测。
> 本汇报覆盖从信号验证到 A10 的全部已完成实验（exp_1 ~ exp_30，每组 5 个 seed）。

---

## 一、TL;DR

- **最佳模型 A8（细+粗+音频，cross-attention 融合）：Test AUC 0.7863 ± 0.0088**，
  与 AFGNN 最强基线（0.797 ± 0.011）的差距落在基线 1 个标准差内，**基本追平**。
- 粗粒度分支的**均值增量两条技术路线均未达显著**（并联 +0.006、FiLM 调制 −0.004），
  但它带来**一致、可复现的稳定性收益**：seed 间波动从 0.0123（无粗图）降到 0.0066~0.0088（有粗图），
  且粗图单独就有 0.66 的真实判别力。
- 两轮关键方法学修正（基线含音频、concat 欠训练）都靠消融抓了出来，
  实验体系的自纠错能力是本阶段最大的隐性产出。

---

## 二、信号验证：先证明有信号，再建模型

在写任何模型代码之前，先对粗描述子做了单变量统计检验
（961 样本，9 区域 × 10 维描述子 × 时序 mean/std 聚合 = 180 个特征）：

- **113/180 个特征 p < 0.05**，信号确认存在且较强；
- 最强单特征 `std(inner_mouth.spread)`：**单变量 AUC = 0.692**，Cohen's d = −0.686，p = 1.5e-27；
- 模式高度一致：top 特征几乎全是**时序标准差**，方向全部是抑郁组更低——
  即"**面部动态范围收窄**"，与抑郁的临床文献（表情活动度下降、面部僵硬）吻合；
- **对称性特征**（左右眉/眼的质心差、速度差的时序波动）|d| ≈ 0.5，
  验证了最初假设：这类信号 68 点细图要跨很多跳才能学到，9 节点粗图上是相邻边，天然易抓。

**这一步的价值**：如果信号不存在，后面所有建模都是浪费。先花半天做统计，
再花一周建模型——这个顺序以后每个项目都值得坚持。

## 三、架构设计的关键取舍

小数据（train 647 样本）是本项目的根本约束，上一个项目 SFAF 的核心教训是：
**小数据上架构复杂度是负收益，只有带来新信息的输入表示才是正收益。**
由此定下三个设计原则：

1. **粗节点 = 零参数手工描述子**（每区域每帧 10 维：质心/面积/散布度/速度/速率/
   运动方差/对称性），不用可学习聚合——把参数预算留给真正需要学习的地方；
2. **复用优先**：细图分支、音频分支、GAT 卷积、训练器全部只读复用 AFGNN，
   HiFAG 自己只写粗图模块和胶水代码（最佳模型总参数仅 151K，小于基线）；
3. **一切可消融**：模态开关、融合方式、特征组、边拓扑全部做成配置开关，
   每组实验 5 个 seed（42~46），结论一律以配对 t 检验为准，不看单次运行。

## 四、实验结果总表

| 配置 | 模态/结构 | Test AUC (mean ± std, 5 seeds) |
|------|-----------|-------------------------------|
| AFGNN 纯面部基线 | 细图（单 seed） | 0.657 |
| AFGNN 完整基线 | 细图+音频+cross-attn | 0.797 ± 0.011 |
| A2 | **粗图单独** | 0.6599 ± 0.0095 |
| A3 | 细+粗（concat） | 0.6709 ± 0.0045 |
| A7 | 细+粗+音频（concat） | 0.7756 ± 0.0085 |
| **A8** | **细+粗+音频（cross-attn）** | **0.7863 ± 0.0088** |
| A9 | 细+音频（cross-attn，**去粗图**） | 0.7805 ± 0.0123 |
| A10 | A8 + FiLM 层级交互 | 0.7824 ± 0.0066 |

### 配对比较（同 seed 相减，t 检验）

| 对比 | 均值差 | 方向一致 seeds | 显著性 | 结论 |
|------|--------|---------------|--------|------|
| A3 − A2（细分支加入） | +0.011 | 4/5 | p ≈ 0.086，边缘显著 | 细分支有小增量，且大幅降低波动 |
| A7 − A3（音频加入） | +0.105 | — | 显然 | 音频是高性能的主要来源 |
| A8 − A7（cross-attn 换 concat） | **+0.0106** | **5/5** | **t ≈ 4.70, p < 0.01** | cross-attention 显著更优 |
| A8 − A9（粗图净增量，完整模态） | +0.0058 | 2/5 | t ≈ 0.85，不显著 | 均值增量小，但波动减半 |
| A10 − A8（FiLM 层级交互） | −0.0039 | 2/5 | t ≈ −0.63，不显著 | **阴性结果**，FiLM 未奏效 |

## 五、过程中的两个方法学发现

### 5.1 基线修正：0.797 不是纯面部基线

一度以为 A2/A3（~0.67）远落后于 AFGNN 基线（0.797），核查配置后发现
**0.797 的基线含音频模态**，纯面部基线只有 0.657。A2/A3 其实都已略超合理基线。
教训：**引用任何 baseline 数字前，先核对其配置文件的每一个开关**——
这个错误如果不抓出来，整个项目的自我定位都会偏。

### 5.2 concat 融合导致音频分支欠训练

A7（concat）有 3/5 个 seed 的最佳验证集表现出现在第 3~5 个 epoch——
模型还没来得及充分训练就被早停。换成 cross-attention 融合（A8）后，
最佳 epoch 普遍后移到 13~41，训练曲线恢复健康，且 5/5 seeds 一致正增益。
**融合方式不是无关紧要的实现细节：弱融合会拖累强模态分支的学习。**

## 六、阴性结果分析：粗图的均值增量为什么没打出来

这是最诚实也最有信息量的一部分。粗图的两条增量路径都试过了：

- **并联**（concat / cross-attention 里拼上粗图向量，A8 vs A9）：+0.0058，不显著；
- **层级交互**（粗图节点嵌入 FiLM 调制细图输入，A10 vs A8）：−0.0039，不显著。

事后看，失败原因可能在**信号粒度的不匹配**：sanity check 早就显示粗描述子的
主信号全是**时序标准差**——"整个视频里表情动态范围收窄"是一个**样本级全局属性**，
而不是逐帧变化的局部信号。帧级 FiLM 调制在粒度上就错了；而样本级的并联注入，
信号又容易被 320 维的细图+音频表示淹没。

**但粗图并非没有价值**，它有三块坚实的立足点：

1. **判别力真实存在**：15.8K 参数的粗图单独达到 0.66，超过纯面部基线 0.657；
2. **稳定性收益一致可复现**：三轮完整模态实验中，
   seed 间波动 0.0123（无粗图）→ 0.0088（并联）→ 0.0066（FiLM），
   对小数据集上的论文结论可靠性至关重要；
3. **可解释性**：描述子直接对应临床概念（表情动态范围、面部不对称性），
   这是纯深度学习分支给不了的叙事。

## 七、下一步计划（已预登记）

A4~A6 消融（exp_31~45，配置和代码均已就绪），目的：**把"粗图学到了什么"讲清楚**。

| 对比 | 回答的问题 |
|------|-----------|
| A2 − A4（去对称性特征） | 对称性信号的净贡献（sanity 中 |d|≈0.5 的那批特征） |
| A2 − A5（只留几何特征） | 运动/动态特征的净贡献 |
| A6 − A2（全连接边 vs 解剖学边） | 解剖学先验是否有用 |

之后按结果择路：样本级粗信号注入（针对粒度问题的新尝试），
或接受"粗图 = 稳定性 + 可解释性组件"的定位，转入论文写作。

## 八、工程沉淀

- 实验管理自动化（exp_N 编号 + 配置/权重/历史自动存档），30 组实验零混乱；
- 特征维度契约断言（禁止静默截断）——源自 SFAF 被静默丢特征误导一个实验结论的教训；
- 新结构 zero-init 起步（FiLM 从"无调制"出发学习，保证不退化）+ 单元测试验证等价性；
- 冒烟测试 16 项全绿才允许上真实数据训练；
- 阴性结果完整记录，不粉饰。
