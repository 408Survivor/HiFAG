# HiFAG: Hierarchical Facial-Audio Graph Network

> 基于 AFGNN 的层级化升级设计文档。
> 本目录为独立的新项目，不修改 `AFGNN/` 内任何文件。

---

## 1. 项目定位

HiFAG 在 AFGNN 的 68-landmark 细粒度面部图之上，增加一个 **9-region 粗粒度面部图**，构成「细粒度 + 粗粒度」的层级面部建模，并与音频分支融合，用于 D-Vlog 抑郁检测。

**核心动机**：68 点细图的 GNN 擅长局部几何关系，但以下三类信号天然难学：

1. **区域级整体状态**：睁眼面积、嘴唇张开度等需要区域内所有点协同计算的量；
2. **左右不对称性**：左眼 vs 右眼、左眉 vs 右眉的运动差异（抑郁文献中的常见线索）；
3. **区域间协调/同步**：下颌-嘴唇说话耦合、眉-眼联动——9 节点图上是相邻边，68 点图上要跨很多跳。

粗粒度层就是为这三类信号设计的。

**与 SFAF 的关系**：SFAF 的教训是「架构复杂度在小数据上是负收益，只有带来新信息的输入表示是正收益」。HiFAG 因此选择**零参数的手工区域描述子**作为粗节点内容，而不是可学习模块堆叠。

---

## 2. 区域划分（复用 AFGNN 定义）

`AFGNN/src/models/graph_utils.py:19-29`，9 个区域：

| region_id | 名称 | landmark 索引 |
|---|---|---|
| 0 | face_contour | 0-16 |
| 1 | left_eyebrow | 17-21 |
| 2 | right_eyebrow | 22-26 |
| 3 | nose_bridge | 27-30 |
| 4 | nose_bottom | 31-35 |
| 5 | left_eye | 36-41 |
| 6 | right_eye | 42-48 |
| 7 | outer_mouth | 48-59 |
| 8 | inner_mouth | 60-67 |

---

## 3. 核心架构

```text
landmark 序列 (T, 68, 2)
        │
        ├──────────────────────→ 细粒度分支（AFGNN FaceGNN，复用）
        │                              │
        └──→ region_features.py ──→ 粗粒度分支（9 节点时空图 + GAT）
        │    手工区域描述子                │
        │                                │
        └──── 层级交互（后期）←───────────┘
                                       │
音频序列 (T_a, 25) ──→ AFGNN AudioGNN ──┤
                                       ▼
                              融合（concat 起步）→ MLP 分类头
```

### 3.1 粗粒度节点特征（方案 A：手工描述子）

每帧、每区域一个节点，特征从 landmark 序列确定性计算，零参数：

**几何特征**（每区域）：
- 质心 `(cx, cy)`（2 维）
- 区域面积（封闭轮廓：眼、外唇、内唇、轮廓多边形；链状区域：鼻梁/鼻底/眉用质心到点平均距离替代）（1 维）
- 区域内散布度：区域内 landmark 到质心的平均距离（1 维）

**运动特征**（每区域）：
- 区域平均速度 `(vx, vy)`（2 维）
- 平均速率 `mean(|v|)`（1 维）
- 区域内运动方差（协调性的反面）（1 维）

**对称性特征**（左右成对区域：眉 1/2、眼 5/6）：
- 左右质心距离（1 维）
- 左右速度差幅值（1 维）
- 挂在左/右两个节点上，其余区域填 0（或作为两者间的边特征，待定）

**合计**：约 10 维/节点/帧（对称性特征如何挂载待实现时定）。

### 3.2 粗粒度图结构

- 节点：9 区域 × T 帧 = 9T 节点/样本（T=32 时 288 节点，比细图小一个量级）
- 空间边（帧内）：
  - 起步方案：解剖学邻接（眉-眼、眼-鼻、鼻-嘴、轮廓-各区域、外唇-内唇）
  - 备选：9 节点全连接（图很小，两种都试，配置切换）
- 时间边：同区域相邻帧双向边（与 AFGNN 一致）

### 3.3 粗粒度编码器

9T 节点规模很小，计算预算宽松：

- 起步：2~3 层 GAT（复用 AFGNN `WeightedGATConv`）+ attention readout
- 备选（全部做成配置开关）：
  - Transformer over (region, time) tokens——token 数少，无 OOM 风险
  - 区域速度的频带分解（SFAF 唯一验证有效的组件；区域信号更长程平稳，频带语义更合理）

### 3.4 细粒度分支

直接复用 AFGNN FaceGNN（含 region one-hot 13 维输入），通过 `paths.py` 导入，不复制代码。

### 3.5 层级交互（第二阶段，方案已定 2026-07-28）

**已定方案：coarse→fine FiLM 输入级调制（单向）**。

背景：A9 消融证明并联接入下粗分支均值增量不显著（+0.0058，t≈0.85），
层级交互是放大粗图贡献的关键路径。两个硬约束决定了方案空间：

- FaceGNN 只读复用、只暴露图级输出 → 不复制代码时唯一注入点是细图输入特征 `x`；
- fine→coarse 方向需要细图节点嵌入，必须复制 FaceGNN 代码，违背工程原则 1，排除。

机制：

1. RegionGNN 先跑一遍，取**节点级**嵌入 h_c（readout 之前，每图 9T×64）；
   RegionGNN 增加 `return_nodes` 开关。
2. 细节点 (t, landmark ℓ) 按固定 landmark→region 映射 gather 所属粗节点嵌入；
   映射表（68 维索引）由 `LANDMARK_GROUPS_68` 预计算为 buffer。
3. FiLM 调制：γ, β = Linear(64→4)，**只调制几何 4 维 `[x, y, dx, dy]`**，
   region one-hot 不动：`x' = x·(1+γ) + β`。
4. γ/β 的最后一层 **zero-init**，模型从"无调制"（≈A8）出发学习，保证不退化。
5. 粗图 readout 并联路径**保留**（不丢 A8 的稳定性收益）；
   RegionGNN 梯度两路回传（readout + 调制），端到端训练，不冻结。

参数增量：2×(64×4+4) ≈ **520**，几乎免费。
配置开关：`hierarchical: "none" | "coarse_to_fine_film"`（默认 none，向后兼容）。
首个实验编号 **A10**（`hifag_a10_full_xattn_film.yaml` = A8 + film 开关）。

备选（若 A10 增量仍不显著）：coarse→fine 特征拼接（h_c 投影 16 维 concat 到
细节点输入，13→29 维）；再不行才考虑复制 FaceGNN 做双向。

### 3.6 融合与分类

- 起步：细图向量 + 粗图向量 + 音频向量 concat → MLP
- 损失 / 早停沿用当前最佳配方：focal α=0.25 + AUC 早停

---

## 4. 关键设计决策

| 决策点 | 起步方案 | 备选 |
|---|---|---|
| 粗节点特征 | 手工几何+运动+对称性描述子 | 学习聚合（方案 B/C，后期） |
| 粗图空间边 | 解剖学邻接 | 全连接 |
| 粗图编码器 | GAT | Transformer / +频带分解 |
| 层级交互 | coarse→fine FiLM 输入调制（A10） | coarse→fine 特征拼接；双向（需复制 FaceGNN，暂不） |
| 融合 | cross-attention（A8 起） | concat |

---

## 5. 验证路线（先验证信号，再建模型）

1. `region_features.py` 实现描述子计算 + sanity check：区域运动能量/面积在抑郁 vs 非抑郁上的分布差异（t-test / 可视化）——**信号不存在就不建模型**；
2. 粗图单独跑：9 节点时空图 + GAT，看单独 Test AUC；
3. 细图 + 粗图 concat：看相对 AFGNN 基线（0.797 ± 0.011）有无增量；
4. 多 seed（42~46）确认。

## 6. 消融设计

| 编号 | 消融内容 | 目的 |
|---|---|---|
| A1 | 细图单独（= AFGNN 基线复现） | 对照 |
| A2 | 粗图单独 | 粗粒度自身信息量 |
| A3 | 细+粗 concat | 核心假设：粗粒度有增量 |
| A4 | 去掉对称性特征 | 对称性贡献 |
| A5 | 去掉运动特征（只留几何） | 运动贡献 |
| A6 | 解剖学边 vs 全连接 | 粗图拓扑 |
| A7 | 音频开/关 | 模态贡献 |
| A8 | cross-attention 融合 | 融合方式（已实现，0.7863±0.0088） |
| A9 | 去粗分支（fine+audio+xattn） | 粗分支在完整模态下的净增量（+0.0058，不显著） |
| A10 | coarse→fine FiLM 层级交互 | 层级结构能否放大粗图贡献（第二阶段） |

---

## 7. 工程原则（继承 SFAF 教训）

1. AFGNN 只读复用，sys.path 收敛到 `src/hifag/paths.py` 一处；
2. 每个可消融组件做成配置开关，`builders.py` 工厂组装；
3. 特征契约显式化：模型入口断言节点特征维度，禁止静默截断（SFAF region one-hot 教训）；
4. 实验目录自动管理 `exp_N`，配置/摘要/历史自动保存；
5. 失败实验也记入 PROGRESS.md；
6. 冒烟测试先行，再跑真实训练。

---

## 8. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 粗描述子无判别信号 | 第 5 节第 1 步先做 sanity check，信号不存在就换节点内容（学习聚合/AU） |
| 粗图与细图信息冗余 | A4/A5 消融定位贡献来源；对称性/区域统计是细图难学的，优先保留 |
| 800 样本过拟合 | 粗图参数预算 < 细图；强 dropout；AUC 早停；多 seed 验证 |
| landmark 噪声污染描述子 | 描述子先做时序平滑（滑动平均）再计算 |
